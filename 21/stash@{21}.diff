diff --git a/src/pages/MyMove/Home/index.jsx b/src/pages/MyMove/Home/index.jsx
index b0e7a67889..c31e581cfd 100644
--- a/src/pages/MyMove/Home/index.jsx
+++ b/src/pages/MyMove/Home/index.jsx
@@ -376,15 +376,16 @@ export class Home extends Component {
           filename = matches[1].replace(/['"]/g, '');
         }
         link.setAttribute('download', filename);
+        link.setAttribute('test-id', 'the-aoa-download-button');

-        try {
-          // Append to html link element page
-          // Hack alert: wrap around try/catch for unit testing,
-          // document.body not available during standalone unit testing testing
-          document.body.appendChild(link);
-        } catch (e) {
-          console.log(e);
-        }
+        //try {
+        // Append to html link element page
+        // Hack alert: wrap around try/catch for unit testing,
+        // document.body not available during standalone unit testing testing
+        document.body.appendChild(link);
+        //} catch (e) {
+        //console.log(e);
+        //}

         // Start download
         link.click();
@@ -461,6 +462,43 @@ export class Home extends Component {
     const currentLocation = current_location;
     const shipmentNumbersByType = {};

+    const linkToDownloadAOA = <downloadLinkAOA test="something" />;
+
+    const advanceRequestStatuses = (shipments) => {
+      const items = React.Children.toArray(
+        shipments
+          .map((shipment) => {
+            const { shipmentType } = shipment;
+            if (shipmentNumbersByType[shipmentType]) {
+              shipmentNumbersByType[shipmentType] += 1;
+            } else {
+              shipmentNumbersByType[shipmentType] = 1;
+            }
+            const shipmentNumber = shipmentNumbersByType[shipmentType];
+            return (
+              <>
+                <strong>
+                  {shipmentTypes[shipment.shipmentType]}
+                  {` ${shipmentNumber} `}
+                </strong>
+                {shipment?.ppmShipment?.advanceStatus === ADVANCE_STATUSES.APPROVED.apiValue && (
+                  <>
+                    <p>test</p>
+                    <p className={styles.downloadLink}>{linkToDownloadAOA}</p>
+                  </>
+                )}
+                {shipment?.ppmShipment?.advanceStatus === ADVANCE_STATUSES.REJECTED.apiValue && (
+                  <Description>Advance request denied</Description>
+                )}
+                {shipment?.ppmShipment?.advanceStatus == null && <Description>Advance request pending</Description>}
+              </>
+            ).props.children;
+          })
+          .flat(),
+      );
+      return items;
+    };
+
     return (
       <>
         <ConnectedDestructiveShipmentConfirmationModal
@@ -638,38 +676,7 @@ export class Home extends Component {
                               <br />
                               <br />
                             </Description>
-                            {ppmShipments.map((shipment) => {
-                              const { shipmentType } = shipment;
-                              if (shipmentNumbersByType[shipmentType]) {
-                                shipmentNumbersByType[shipmentType] += 1;
-                              } else {
-                                shipmentNumbersByType[shipmentType] = 1;
-                              }
-                              const shipmentNumber = shipmentNumbersByType[shipmentType];
-                              return (
-                                <>
-                                  <strong>
-                                    {shipmentTypes[shipment.shipmentType]}
-                                    {` ${shipmentNumber} `}
-                                  </strong>
-                                  {shipment?.ppmShipment?.advanceStatus === ADVANCE_STATUSES.APPROVED.apiValue && (
-                                    <p className={styles.downloadLink}>
-                                      <Link
-                                        onClick={() => this.handlePPMAOAPacketDownloadClick(shipment?.ppmShipment.id)}
-                                      >
-                                        Download AOA Paperwork (PDF)
-                                      </Link>
-                                    </p>
-                                  )}
-                                  {shipment?.ppmShipment?.advanceStatus === ADVANCE_STATUSES.REJECTED.apiValue && (
-                                    <Description>Advance request denied</Description>
-                                  )}
-                                  {shipment?.ppmShipment?.advanceStatus == null && (
-                                    <Description>Advance request pending</Description>
-                                  )}
-                                </>
-                              );
-                            })}
+                            <>{advanceRequestStatuses(ppmShipments)}</>
                           </>
                         )}
                         {this.hasAllAdvancesRejected && (
diff --git a/src/pages/MyMove/Home/index.test.jsx b/src/pages/MyMove/Home/index.test.jsx
index 7a6d93dc73..8c4d66b45b 100644
--- a/src/pages/MyMove/Home/index.test.jsx
+++ b/src/pages/MyMove/Home/index.test.jsx
@@ -686,14 +686,14 @@ describe('Home component', () => {
         const mtoShipments = [approvedAdvancePPMShipment];
         const props = { ...defaultProps, ...propUpdates, mtoShipments };

-        render(
+        const root = render(
           <MemoryRouter>
             <Home {...props} />
           </MemoryRouter>,
         );

+        const header = screen.debug(screen.getByText('Advance request submitted'));
         expect(screen.getByText('Download AOA Paperwork (PDF)')).toBeInTheDocument();
-
         const downloadAOAButton = screen.getByText('Download AOA Paperwork (PDF)');
         expect(downloadAOAButton).toBeInTheDocument();

@@ -708,16 +708,17 @@ describe('Home component', () => {
             },
           };
         }
-        let anchor = makeAnchor({ href: '#', download: '' });
-        let createElementMock = jest.spyOn(document, 'createElement').mockReturnValue(anchor);
-        let clickSpy = jest.spyOn(anchor, 'click');
+        //let anchor = makeAnchor({ href: '#', download: '', id: 'the-anchor' });

-        // jest.spyOn(document.body, 'appendChild').mockReturnValue(anchor);
+        //let createElementMock = jest.spyOn(document, 'createElement').mockReturnValue(anchor);
+        screen.debug(downloadAOAButton);
+        //let clickSpy = jest.spyOn(anchor, 'click');

+        // jest.spyOn(document.body, 'appendChild').mockReturnValue(anchor);
         await userEvent.click(downloadAOAButton);
-
+        const anchor = screen.getByTestId('the-aoa-download-button');
         // verify hyperlink was created
-        expect(document.createElement).toBeCalledWith('a');
+        //expect(document.createElement).toBeCalledWith('a');

         // // verify hypelink element was created with correct
         // // default file name from content-disposition
@@ -736,7 +737,7 @@ describe('Home component', () => {
             ),
           ).toThrow();
         });
-        createElementMock.mockRestore(); // restore document.createElement so that it is unchanged in other tests
+        //createElementMock.mockRestore(); // restore document.createElement so that it is unchanged in other tests
       });
     });

